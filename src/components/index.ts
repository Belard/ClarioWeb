/**
 * Components — barrel export.
 *
 * Re-exports from sub-directories:
 *   ui/      → base UI primitives (Button, Input, …)
 *   layout/  → structural layout pieces (Header, Footer, Sidebar, …)
 */

export * from './ui';
export * from './layout';
